{% for category in portfolio.categories %}
  {% if category.name == project.category %}
    {% if links == true %}
      <a href='{{ "/categories/" | relativeLink }}{{ category.name | slug }}/' class='category' style='background-color: {{ category.color }}'>{{ category.name }}</a>
    {% else %}
      <p class='category' style='background-color: {{ category.color }} '>{{ category.name }}</p>
    {% endif %}
  {% endif %}
{% endfor %}

{% for tag in portfolio.tags %}
  {% for projTag in project.tags %}
    {% if tag.name == projTag %}
      {% if links == true %}
        <a href='{{ "/tags/" | relativeLink }}{{ tag.name | downcase }}/' class='tag' style='background-color: {{ tag.color }}'>{{ tag.name }}</a>
      {% else %}
        <p class='tag' style='background-color: {{ tag.color }} '>{{ tag.name }}</p>
      {% endif %}
    {% endif %}
  {% endfor %}
{% endfor %}
