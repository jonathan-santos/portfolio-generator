---
pagination:
  data: portfolio.tags
  size: 1
  alias: tag
permalink: '/tags/{{ tag.name | slug }}/'
---

<!DOCTYPE HTML>
<html lang='en'>

{% include head, title: tag.name %}

<body>
  <main>
    {% include back-button %}

    {% include area-title, title: tag.name %}

    {% include filters, selected: 'tag', filter: tag %}

    <div class='projects-area'>
      {% for project in portfolio.projects %}
        {% for ProjectTag in project.tags %}
          {% if ProjectTag == tag.name %}
            {% include project %}
          {% endif %}
        {% endfor %}
      {% endfor %}
    </div>
  </main>

  {% include footer %}
</body>

</html>
