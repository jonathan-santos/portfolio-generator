<!DOCTYPE HTML>
<html lang='{{ lang }}'>

{% include sections/head, title: project.name %}

<body>
  <main class='page-project'>
    {% include components/back-button %}

    {% include components/language-selector, selected: lang %}

    {% include sections/area-title-project %}

    {% include sections/project-content %}
  </main>

  {% include sections/footer %}
</body>

<script>
  if (document.fullscreenEnabled) {
    document.querySelectorAll('.project-content__images li')
      .forEach(function (li) {
        li.children[0].tabIndex = '0'
        li.children[0].classList.remove('no-interaction')
  
        li.addEventListener('click', function() {
          li.requestFullscreen({ navigationUI: 'show' })
        })
      })
  }
</script>

</html>
